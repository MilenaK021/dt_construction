version: "3.9"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: dtwin
      POSTGRES_USER: dtuser
      POSTGRES_PASSWORD: dtpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
